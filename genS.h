#ifndef __GENS_H__
#define __GENS_H__

#include <stdio.h>	/* fopen(), fclose(), fprintf(), sprintf() */
#include <string.h>	/* strlen() */

#include "loadf.h"

int genS(const char *fname, char *program);

#endif
